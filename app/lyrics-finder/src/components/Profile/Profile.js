import { CgProfile } from 'react-icons/cg';

const Profile = () => {
  return (
    <section className='profile'>
      <CgProfile className='profile-image' size={70} />
    </section>
  );
};

export default Profile;
